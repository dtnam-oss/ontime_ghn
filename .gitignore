node_modules/
dist/
build/
.env
.env.local
.env.*.local

# Service account / secrets — NEVER commit
service-account*.json
*credentials*.json
nakvn-*.json
/*.json          # mọi file .json ở thư mục gốc (nơi hay đặt key) — package.json nằm trong sync/ web/ nên an toàn
!/vercel.json    # ngoại lệ: vercel.json là config (không phải secret) -> vẫn track
*.key
*.pem

# Fixture dev-only (chứa PII) — chỉ dùng local cho test, KHÔNG commit/deploy
web/src/fixtures/

.DS_Store
*.log
